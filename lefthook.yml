commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit {1}

pre-commit:
  parallel: true
  commands:
    format:
      run: npx prettier --write . && git add -A
    lint:
      run: npx oxlint .
    test:
      run: npm test
